import { ButtonControlledOptionalProps } from "../types";

const buttonDefaultProps: Required<ButtonControlledOptionalProps> = {
  size: "md",
  variant: "default",
  icon: null,
};

export default buttonDefaultProps;
